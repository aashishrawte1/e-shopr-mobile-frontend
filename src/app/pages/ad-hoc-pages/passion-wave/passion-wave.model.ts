export interface IPassionWaveExcelSheetData {
  Timestamp: string;
  'Enter User Email (Please ensure its the same email as the one used for registering on GreenDay)': string;
  'Enter District': string;
  'Enter Weight (kg)': number;
}
